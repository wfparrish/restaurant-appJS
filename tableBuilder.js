let assignedTable;

function tableBuilder() {
  let table0 = new Table(
    1,
    party,
    seatArray,
    checkArray,
    orderArray,
    userLogArray
  );
  return table0;
}

assignedTable = new tableBuilder();
